Mongo Database Tools
https://www.mongodb.com/try/download/database-tools

Instructions:
1. Open Command Line in this /db_setup as Administrator
2. Run the command depending on the function.

To Export:
    If localhost
        .\mongodump --uri mongodb://localhost:27017/hris-leonio-group

To Import
    If localhost
        .\mongorestore --uri mongodb://localhost:27017/